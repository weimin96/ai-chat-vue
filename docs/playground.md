# Playground

本项目使用 `play/` 作为本地组件开发入口。它与文档站职责不同：

- `play/`：面向开发者，用于临时调试和验证组件源码。
- `docs/`：面向使用者，用于阅读说明、查看 API 和运行示例。

启动本地调试：

```powershell
npm run dev
```

构建本地调试入口：

```powershell
npm run play:build
```
